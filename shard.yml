name: mongo-log-replay
version: 0.1.0

authors:
  - Adrien Rey-Jarthon <jobs@adrienjarthon.com>

targets:
  mongo-log-replay:
    main: mongo-log-replay.cr

dependencies:
  mongo:
    github: datanoise/mongo.cr
    branch: master

crystal: 0.27.2

license: MIT
